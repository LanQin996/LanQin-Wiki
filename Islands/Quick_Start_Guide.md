# Islands 插件快速入门指南

## 🚀 5分钟快速上手

### 第一步：安装依赖插件
1. **必需**: 安装 `Multiverse-Core` 插件
2. **推荐**: 安装 `PlaceholderAPI` 和 `Vault` 插件
3. 重启服务器确保依赖插件正常加载

### 第二步：安装Islands插件
1. 下载 `Islands.jar` 文件
2. 放入 `plugins` 文件夹
3. 重启服务器
4. 检查控制台是否有错误信息

> ⚠️ **重要提醒**: Islands插件现在强制依赖于Multiverse-Core。如果没有安装Multiverse-Core，插件将无法正常启动，控制台会显示错误信息。

### 第二步：基础配置
1. 编辑 `plugins/Islands/config.yml`
2. 根据需要调整基础设置
3. 执行 `/islands reload` 重载配置

### 第三步：创建第一个空岛
1. 玩家执行 `/islands create`
2. 系统自动生成空岛世界
3. 使用 `/islands` 传送到空岛

---

## 📋 常用命令速查

### 基础操作
| 命令 | 功能 | 权限 |
|------|------|------|
| `/islands` | 传送到空岛 | 需要空岛 |
| `/islands create` | 创建空岛 | `islands.create` |
| `/islands help` | 查看帮助 | 无 |

### 团队管理
| 命令 | 功能 | 权限 |
|------|------|------|
| `/islands invite <玩家>` | 邀请成员 | `islands.invite` |
| `/islands accept` | 接受邀请 | `islands.invite` |
| `/islands kick <玩家>` | 踢出成员 | `islands.kick` |
| `/islands leave` | 离开团队 | `islands.leave` |

### 岛屿设置
| 命令 | 功能 | 权限 |
|------|------|------|
| `/islands lock` | 锁定/解锁 | `islands.lock` |
| `/islands pvp` | PVP开关 | `islands.pvp` |
| `/islands sethome` | 设置出生点 | `islands.sethome` |
| `/islands border` | 边界显示 | `islands.border` |

---

## 🎮 游戏流程

### 新手玩家流程

##### 1. 创建空岛
```bash
/islands create
```
- 系统自动生成空岛世界
- 设置基础边界
- 创建团队

##### 2. 邀请朋友
```bash
/islands invite 朋友名字
```
- 朋友会收到邀请消息
- 朋友执行 `/islands accept` 接受

##### 3. 管理岛屿
```bash
/islands lock          # 锁定岛屿
/islands sethome       # 设置出生点
/islands border        # 显示边界
```

##### 4. 升级岛屿
- 收集足够的金币和点券
- 使用升级命令扩展边界
- 获得更多功能

### 团队成员流程

#### 1. 接受邀请
```bash
/islands accept
```
- 加入空岛团队
- 获得成员权限

#### 2. 传送到空岛
```bash
/islands
```
- 传送到团队空岛
- 开始生存

#### 3. 离开团队
```bash
/islands leave
```
- 退出当前团队
- 可以加入其他团队

---

## ⚙️ 配置要点

### 重要配置项

#### 基础设置
```yaml
Island:
  # 岛屿高度
  IslandLevel: 120
  # 最大等级
  MaxLevel: 10
  # 基础边界半径
  WorldBoard: 64
  # 升级半径增量
  UpdateRadius: 50
```

#### 权限设置
```yaml
Permission:
  create: true    # 创建空岛
  invite: true    # 邀请成员
  kick: true      # 踢出成员
  lock: true      # 锁定岛屿
  pvp: true       # PVP控制
```

#### 经济设置
```yaml
Island:
  MoneyNeed:      # 升级金币需求
    - 5000
    - 10000
    - 15000
  PointsNeed:     # 升级点券需求
    - 100
    - 150
    - 200
```

---

## 🛠️ 管理员指南

### 安装后检查
1. 检查插件是否正常加载
2. 测试基础命令功能
3. 确认权限节点正确
4. 测试PAPI变量

### 常见配置调整

##### 调整岛屿大小
```yaml
Island:
  WorldBoard: 64        # 基础半径
  UpdateRadius: 50      # 每次升级增加的半径
```

##### 调整升级费用
```yaml
Island:
  MoneyNeed:
    - 1000    # 1级升2级
    - 2000    # 2级升3级
    - 3000    # 3级升4级
```

##### 调整权限
```yaml
Permission:
  create: false    # 禁用创建功能
  invite: true     # 允许邀请
```

### 数据管理
- 团队数据存储在 `plugins/Islands/teams.yml` 目录
- 个人数据存储在 `plugins/Islands/user.yml` 目录
- 岛屿世界存储在 `world/` 目录
- 定期备份数据文件
- 使用 `/islands reload` 重载配置

---

## 🚨 故障排除

### 常见启动问题

#### 插件无法启动
如果控制台显示以下错误：
```
[Islands] Multiverse-Core not found! Plugin will be disabled!
```
**解决方案**: 安装Multiverse-Core插件

#### 依赖插件版本问题
- 确保Multiverse-Core版本与服务器版本兼容
- 推荐使用最新稳定版本

### 获取帮助
```bash
/islands help          # 查看帮助
/islands help 2        # 查看第2页
```

### 权限检查
```bash
/papi parse me %islands_level%    # 测试PAPI变量
/papi list islands                # 查看所有变量
```

### 配置文件
- `config.yml` - 主要配置
- `messages.yml` - 消息配置
- `help.yml` - 帮助配置

---

## 🎯 进阶功能

### 多模板系统
- 支持多种空岛模板
- 不同模板有不同的起始结构
- 可在创建时选择模板

### 边界系统
- 可配置的边界伤害
- 边界警告距离
- 边界可视化显示

### 经济集成
- 支持Vault经济插件
- 支持PlayerPoints点券
- 升级费用配置

### PAPI变量
- 丰富的占位符变量
- 支持计分板、聊天格式
- 支持GUI界面